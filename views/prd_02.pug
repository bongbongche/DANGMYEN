extends layouts/sub

block sub
    .product__title.sub__title
        h1
        p
        ul
           li
               a(href=`/product${routes.prd_01}`) 푸름찬 당면
           li.on
               a(href=`/product${routes.prd_02}`) 자매품
    section.product__title_prd_02